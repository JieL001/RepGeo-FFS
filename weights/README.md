# Weights

This directory intentionally does not include large `.pth` files.

Expected original FFS checkpoint path:

```text
weights/23-36-37/model_best_bp2_serialize.pth
```

RepGeo-FFS checkpoints should be generated by the training/export scripts or copied from local experiment outputs.


